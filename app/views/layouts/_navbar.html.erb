<nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6 rounded-lg ml-3 mr-3 mt-2 mb-2">
  <%= link_to root_path do %>
    <div class="flex items-center flex-shrink-0 text-white mr-6">
      <span class="fas fa-lg fa-user-slash"></span>
      <span class="ml-1 font-bold text-xl tracking-tight">myxer</span>
    </div>
  <% end %>
  <div class="block lg:hidden">
    <button class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
      <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
    </button>
  </div>
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
    <% if user_signed_in? %>
      <div class="text-sm lg:flex-grow">
        <%= link_to 'My Profile', user_path(current_user), class: "block mt-4 lg:inline-block lg:mt-0 text-teal-100 hover:text-white mr-4" %>
        <%= link_to 'Inbox', chats_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-100 hover:text-white mr-4" %>
      </div>
    <% end %>
    
  </div>
  <div>
    <% if user_signed_in? %>
      <span class="text-teal-100 font-bold mr-3"><%= current_user.username %></span>
      <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0' %>
    <% else %>
      <button class='inline-block text-sm px-4 py-2 leading-none rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0 sign_in' id="sign_in">Sign In</button>
      <button class='inline-block text-sm px-4 py-2 leading-none rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0 sign_up' id="sign_up">Sign Up</button>
    <% end %>
  </div>
</nav>